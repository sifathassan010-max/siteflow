export default function DashboardPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-neutral-500">Dashboard placeholder — built in step 8 (unified dashboard).</p>
    </main>
  );
}
